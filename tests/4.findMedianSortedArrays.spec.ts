import { expect, test } from 'vitest';
import { findMedianSortedArrays } from '../ts/4.findMedianSortedArrays';

test.only('findMedianSortedArrays', () => {
  // expect(findMedianSortedArrays([1, 3], [2, 4])).toEqual(2);
});
